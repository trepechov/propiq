# Tools Directory

Reusable scripts and testing resources.

## Scripts (`/scripts/`)

Utility scripts for common operations.

| Script | Description |
|--------|-------------|
| `example-script.ts` | Example - delete this row when adding real scripts |

## Test Requests (`/testing/requests/`)

QA test request documents. Use `/test` command to create new ones.

| Request | Description |
|---------|-------------|
| `example-request.md` | Example - delete this row when adding real requests |

## Directory Structure

```
tools/
├── scripts/          # Utility scripts (version controlled)
├── testing/
│   ├── requests/     # Test request docs (version controlled)
│   └── results/      # Test reports (gitignored)
└── tmp/              # Disposable files (gitignored)
```
